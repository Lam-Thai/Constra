# Neon Postgres connection string. Get the real value from the Neon
# project dashboard (Connection Details), then copy this file to
# .env.local and paste it in there. Never commit the real value.
DATABASE_URL="postgresql://user:password@ep-xxxx.region.aws.neon.tech/dbname?sslmode=require"
